import React from "react";
import {
  Flex,
  Text,
  Icon,
  Link,
  Menu,
  MenuButton,
  MenuList,
} from 'chakra-ui/react';

const NavItem = ({ navSize }) => {
  return (
    <Flex
      mt={30}
      flexDir="column"
      width="100%"
      alignItems="center"
    >

    </Flex>
  );
}

export default NavItem;
